import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color.fromARGB(255, 115, 201, 216);
  static const Color orange = Color(
    0xFFFF8746,
  ); // Màu cam (cho task quan trọng)
  static const Color pink = Color(0xFFFF4667); // Màu hồng (cho cảnh báo)
  static const Color grey = Color(0xFF121212); // Màu đen xám (Text)
  static const Color greyText = Color(0xFFBDBDBD); // Màu xám nhạt (Note)
  static const Color background = Color(0xFFF8F9FA);
}
